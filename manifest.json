{
  "schemaVersion": 1,
  "id": "steelseries.mouse-controller",
  "name": "SteelSeries Mouse Controller",
  "version": "0.3.0",
  "author": "tantuyu",
  "license": "MIT",
  "description": "Battery, DPI presets, polling rate, and button mapping for a SteelSeries Rival 3 Gen 2 in the Omarchy bar (via rivalcfg).",
  "kinds": [
    "bar-widget"
  ],
  "entryPoints": {
    "barWidget": "BarWidget.qml"
  },
  "barWidget": {
    "displayName": "SteelSeries Mouse Controller",
    "category": "Hardware",
    "allowMultiple": false,
    "defaultSection": "right",
    "defaults": {
      "pollInterval": 15,
      "showPercentage": true
    },
    "schema": [
      {
        "key": "showPercentage",
        "type": "boolean",
        "label": "Show battery percentage",
        "description": "Show battery percentage next to the mouse icon on the bar.",
        "defaultValue": true
      },
      {
        "key": "pollInterval",
        "type": "number",
        "label": "Poll interval (seconds)",
        "description": "How often to query the mouse battery over HID (rivalcfg).",
        "defaultValue": 15
      }
    ]
  }
}
